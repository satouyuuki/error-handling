<h1>エラーハンドリング</h1>

<% @users.each do |user| %>
<p>nama: <%= user.name %> email: <%= user.email %></p>
<% end %>

<%= form_tag do %>
  <%= label_tag(:q, "お名前") %>
  <%= text_field_tag(:q) %>
  <%= submit_tag("送信") %>
<% end %>
